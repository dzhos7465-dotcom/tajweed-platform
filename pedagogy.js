/* ══════════════════════════════════════════════════════════════════════
   ПЕДАГОГИЧЕСКИЕ КОНСТАНТЫ  (pedagogy.js)
   ──────────────────────────────────────────────────────────────────────
   ОТДЕЛЬНЫЙ СЛОЙ между библиотекой знаний и дизайн-системой.

   Здесь живёт то, что относится к МЕТОДИКЕ преподавания, а не к самому
   знанию и не к оформлению интерфейса.

   Ключевой принцип, к которому мы пришли:
     • Само правило НЕ имеет цвета. Икляб не «оранжевый по природе».
     • Цвет появляется только в рамках методики преподавания.
     • Поэтому цвет ссылается на тему по id, но НЕ хранится внутри темы.

   Следствие: одну и ту же библиотеку знаний могут использовать разные
   методики — каждая со своим набором педагогических констант, без
   дублирования знаний. «Ссылаемся, а не копируем» на уровне методики.

   В этом слое со временем появятся: обозначения, способы подсветки,
   оформление букв и другие неизменные принципы методики. Пока — цвета.

   Граница «что есть факт, а что — методическая подача» намеренно оставлена
   ОТКРЫТОЙ: уточним, когда реальный экран заставит ответить.
══════════════════════════════════════════════════════════════════════ */


/* ──────────────────────────────────────────────────────────────────────
   ЦВЕТА ПРАВИЛ
   Постоянный цвет каждого правила — педагогический инструмент: ученик
   узнаёт правило не только по названию, но и по цвету. Цвет привязан
   к теме по id. Базой послужила цветовая система из уроков автора.

   Каждая запись: цвет «нуна/мима» (носитель правила) и цвет «буквы»
   (та, что вызывает правило). Пока используется главным образом accent;
   парные цвета сохранены для будущей подсветки букв.
────────────────────────────────────────────────────────────────────── */
const RULE_COLORS = {
  // Правила мима
  izhar_mim:  { accent: '#2563eb', letter: '#dc2626' },
  idgham_mim: { accent: '#64748b', letter: '#16a34a' },
  ikhfa_mim:  { accent: '#009c8e', letter: '#7f1d1d' },
  shadda_mim: { accent: '#c2410c', letter: '#b08428' },

  // Правила нуна
  izhar_nun:  { accent: '#2563eb', letter: '#dc2626' },
  idgham_nun: { accent: '#64748b', letter: '#16a34a' },
  iqlab_nun:  { accent: '#7c3aed', letter: '#f97316' },
  ikhfa_nun:  { accent: '#009c8e', letter: '#7f1d1d' },
  shadda_nun: { accent: '#b08428', letter: '#b08428' },
};

/* Безопасный доступ к цвету темы. Если методика не задала цвет для темы —
   возвращается нейтральный цвет интерфейса (из дизайн-системы), а не ошибка.
   Так знание, у которого ещё нет методического цвета, всё равно отображается. */
function ruleAccent(themeId) {
  return (RULE_COLORS[themeId] && RULE_COLORS[themeId].accent) || 'var(--seal)';
}

function ruleLetterColor(themeId) {
  return (RULE_COLORS[themeId] && RULE_COLORS[themeId].letter) || 'var(--ink)';
}
